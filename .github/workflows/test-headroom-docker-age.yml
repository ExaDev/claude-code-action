# TEMPORARY. Validates the new OCI-based docker release-age check on a real ubuntu-latest runner, and re-confirms the pip branch has no regression. Removed once confirmed.
name: Test Headroom Docker Age Check (temporary)

on:
  pull_request:
    types: [synchronize]

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  test-docker:
    if: github.event.pull_request.head.ref == 'feat/headroom-docker-min-release-age'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - uses: ./
        id: claude
        with:
          mode: generic
          prompt_file: .github/test-headroom-docker-age-prompt.md
          github_token: ${{ github.token }}
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          headroom_enabled: true
          headroom_install_method: docker
          model: haiku
      - name: Report result
        if: always()
        run: |
          {
            echo "### Headroom docker age-check validation"
            echo "conclusion: ${{ steps.claude.outputs.conclusion }}"
            echo "headroom_proxied: ${{ steps.claude.outputs.headroom_proxied }}"
          } >> "$GITHUB_STEP_SUMMARY"

  test-pip-regression:
    if: github.event.pull_request.head.ref == 'feat/headroom-docker-min-release-age'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - uses: ./
        id: claude
        with:
          mode: generic
          prompt_file: .github/test-headroom-docker-age-prompt.md
          github_token: ${{ github.token }}
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          headroom_enabled: true
          headroom_install_method: pip
          model: haiku
      - name: Report result
        if: always()
        run: |
          {
            echo "### Headroom pip regression check"
            echo "conclusion: ${{ steps.claude.outputs.conclusion }}"
            echo "headroom_proxied: ${{ steps.claude.outputs.headroom_proxied }}"
          } >> "$GITHUB_STEP_SUMMARY"
