# TEMPORARY. Validates the new PyPI release-age check runs correctly on a real ubuntu-latest runner (local testing used macOS, not the actual PEP 668 environment this targets). Removed once confirmed.
name: Test Headroom Age Check (temporary)

on:
  pull_request:
    types: [synchronize]

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  test:
    if: github.event.pull_request.head.ref == 'feat/headroom-min-release-age'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - uses: ./
        id: claude
        with:
          mode: generic
          prompt_file: .github/test-headroom-age-prompt.md
          github_token: ${{ github.token }}
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          headroom_enabled: true
          model: haiku
      - name: Report result
        if: always()
        run: |
          {
            echo "### Headroom age-check validation"
            echo "conclusion: ${{ steps.claude.outputs.conclusion }}"
            echo "headroom_proxied: ${{ steps.claude.outputs.headroom_proxied }}"
          } >> "$GITHUB_STEP_SUMMARY"
